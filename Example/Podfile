use_frameworks!

target 'TopicSubscriberService_Example' do
  pod 'TopicSubscriberService', :path => '../'

  target 'TopicSubscriberService_Tests' do
    inherit! :search_paths

    pod 'Quick', '~> 0.8'
  pod 'Nimble', '~> 3.0'
  end
end
